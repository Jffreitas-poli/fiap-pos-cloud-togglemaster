apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: flag-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/Jffreitas-poli/fiap-pos-cloud-togglemaster.git'
    targetRevision: main
    path: 'flag-service-main/k8s'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: toggle
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true